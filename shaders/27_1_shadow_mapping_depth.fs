#version 330 core

void main() {
    // NOTE: We could explicitly set the depth by uncommenting the below line
    // but this is effectively what happens behind the scene anyways.

    // gl_FragDepth = glFragCoord.z;
}